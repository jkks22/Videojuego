{
  "name": "arcane-assembly-server",
  "version": "2.0.0",
  "description": "Servidor backend de Arcane Assembly — TC2005B Equipo 4",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "mysql -u root -p arcane_assembly < db/schema.sql && mysql -u root -p arcane_assembly < db/seed.sql"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.9.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
